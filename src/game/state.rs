//! Game state management

/// Main game struct (placeholder for Phase 1)
#[derive(Debug)]
pub struct Game {
    // Will be implemented in later phases
}

impl Game {
    /// Create a new game instance
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for Game {
    fn default() -> Self {
        Self::new()
    }
}